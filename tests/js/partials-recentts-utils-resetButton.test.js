import { describe, it, expect } from 'vitest';
import * as Module from '@/js/admin/partials/recentts/utils/resetButton.js';
describe('resources/js/admin/partials/recentts/utils/resetButton.js', () => {
    it('should load and be defined', () => {
        expect(Module).toBeDefined();
    });
});
