import { describe, it, expect } from 'vitest';
import * as Module from '@/js/admin/layouts/selectable-table.js';
describe('resources/js/admin/layouts/selectable-table.js', () => {
    it('should load and be defined', () => {
        expect(Module).toBeDefined();
    });
});
