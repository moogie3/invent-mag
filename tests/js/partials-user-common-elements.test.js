import { describe, it, expect } from 'vitest';
import * as Module from '@/js/admin/partials/user/common/elements.js';
describe('resources/js/admin/partials/user/common/elements.js', () => {
    it('should load and be defined', () => {
        expect(Module).toBeDefined();
    });
});
