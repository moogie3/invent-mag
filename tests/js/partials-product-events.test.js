import { describe, it, expect } from 'vitest';
import * as Module from '@/js/admin/partials/product/events.js';
describe('resources/js/admin/partials/product/events.js', () => {
    it('should load and be defined', () => {
        expect(Module).toBeDefined();
    });
});
