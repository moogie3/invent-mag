import { describe, it, expect } from 'vitest';
import * as Module from '@/js/admin/partials/sorting/listjs.js';
describe('resources/js/admin/partials/sorting/listjs.js', () => {
    it('should load and be defined', () => {
        expect(Module).toBeDefined();
    });
});
