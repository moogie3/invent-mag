import { describe, it, expect } from 'vitest';
import * as Module from '@/js/admin/partials/sorting/search.js';
describe('resources/js/admin/partials/sorting/search.js', () => {
    it('should load and be defined', () => {
        expect(Module).toBeDefined();
    });
});
