import { describe, it, expect } from 'vitest';
import * as Module from '@/js/admin/recentts.js';
describe('resources/js/admin/recentts.js', () => {
    it('should load and be defined', () => {
        expect(Module).toBeDefined();
    });
});
