import { describe, it, expect } from 'vitest';
import * as Module from '@/js/admin/partials/auth/togglePasswordVisibility.js';
describe('resources/js/admin/partials/auth/togglePasswordVisibility.js', () => {
    it('should load and be defined', () => {
        expect(Module).toBeDefined();
    });
});
