import { describe, it, expect } from 'vitest';
import * as Module from '@/js/admin/partials/pos/utils/ui.js';
describe('resources/js/admin/partials/pos/utils/ui.js', () => {
    it('should load and be defined', () => {
        expect(Module).toBeDefined();
    });
});
