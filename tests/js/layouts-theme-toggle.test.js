import { describe, it, expect } from 'vitest';
import * as Module from '@/js/admin/layouts/theme-toggle.js';
describe('resources/js/admin/layouts/theme-toggle.js', () => {
    it('should load and be defined', () => {
        expect(Module).toBeDefined();
    });
});
