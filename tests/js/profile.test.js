import { describe, it, expect } from 'vitest';
import * as Module from '@/js/admin/profile.js';
describe('resources/js/admin/profile.js', () => {
    it('should load and be defined', () => {
        expect(Module).toBeDefined();
    });
});
